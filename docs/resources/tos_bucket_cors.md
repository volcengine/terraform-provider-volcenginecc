---
page_title: "volcenginecc_tos_bucket_cors Resource - terraform-provider-volcenginecc"
subcategory: "TOS"
description: |-
  Set CORS (Cross-Origin Resource Sharing) rules for the bucket.
---

# volcenginecc_tos_bucket_cors (Resource)

Set CORS (Cross-Origin Resource Sharing) rules for the bucket.

## Example Usage

```terraform
resource "volcenginecc_tos_bucket_cors" "TOSBucketCorsDemo" {
  bucket_name = "ccapi-test"
  cors_rules = [{
    allowed_origins = ["http://test1.example.com:8080", "http://test2.example.com:8088"]
    allowed_methods = ["PUT", "DELETE"]
    expose_headers  = ["x-tos-version-id"]
    max_age_seconds = 7200
    allowed_headers = ["Content-Type", "Authorization"]
    response_vary   = false
    }, {
    allowed_origins = ["https://*.example.org"]
    allowed_methods = ["HEAD"]
    expose_headers  = ["x-tos-request-id"]
    max_age_seconds = 1800
    allowed_headers = ["*"]
    response_vary   = true
  }]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `bucket_name` (String) Bucket name.
- `cors_rules` (Attributes Set) CORS rule information.
 Important Note: When using SetNestedAttribute, you must fully define all attributes of its nested structure. Incomplete definitions may cause Terraform to detect unexpected differences during plan comparison, triggering unnecessary resource updates and affecting resource stability and predictability. (see [below for nested schema](#nestedatt--cors_rules))

### Read-Only

- `id` (String) Uniquely identifies the resource.

<a id="nestedatt--cors_rules"></a>
### Nested Schema for `cors_rules`

Optional:

- `allowed_headers` (Set of String) Specify the response headers allowed for cross-origin requests.
- `allowed_methods` (Set of String) Specify the allowed methods for cross-origin requests.
- `allowed_origins` (Set of String) Specify the allowed origins for cross-origin requests.
- `expose_headers` (Set of String) Specify additional headers to include in the CORS response to provide extra information to the client.
- `max_age_seconds` (Number) Specify the cache duration for OPTIONS request responses on the client side, in seconds. Default is 3600.
- `response_vary` (Boolean) Whether to return the Vary: Origin header

## Import

Import is supported using the following syntax:

```shell
$ terraform import volcenginecc_tos_bucket_cors.example "bucket_name"
```